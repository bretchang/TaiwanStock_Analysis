# 台股個股財務監測儀表板 — 開發計畫 (plan.md)

## 0. 一句話定義
建立一個**單一 HTML 檔的監測儀表板**，追蹤台股個股的五大財務指標（應收帳款、存貨、合約負債、營收、獲利），依**產業別**分區，並對**大幅變動**的標的自動**醒目標記與排序**。

---

## 1. 專案目標與範圍

### 核心需求
1. 五大指標監測：應收帳款、存貨、合約負債、營收、獲利。
2. 依產業類別分組呈現（半導體、被動元件、PCB 材料、記憶體、光通訊…）。
3. 大幅上升（或下降）時自動醒目標記 + 可依變動幅度排序。

### 刻意排除（先不做，避免範圍蔓延）
- 即時報價／K 線圖（這是另一條資料線，與基本面監測脫鉤）。
- 個股交易下單。
- 多使用者帳號系統（單機自用為主）。

### 架構原則
資料層（Python ETL）與呈現層（HTML）**完全分離**：ETL 把資料整理成乾淨的 JSON，HTML 只負責讀 JSON 並渲染。好處是資料更新不動前端、前端改版不動資料、未來要換資料源只改 ETL。

---

## 2. 資料來源

### 主要：FinMind（推薦）
開源台股財經資料 API，免費額度足夠個人監測用。對應的 dataset：

| 指標 | 報表出處 | FinMind dataset | 頻率 |
|------|---------|----------------|------|
| 營收（月） | 月營收公告 | `TaiwanStockMonthRevenue` | 月 |
| 營收（季）、獲利 | 綜合損益表 | `TaiwanStockFinancialStatements` | 季 |
| 應收帳款、存貨、合約負債 | 資產負債表 | `TaiwanStockBalanceSheet` | 季 |
| 產業別、股票名稱 | 基本資料 | `TaiwanStockInfo` | 靜態 |

### 備援／核對：MOPS 公開資訊觀測站
官方原始財報，科目最完整（合約負債等 IFRS 細項一定有）。FinMind 抓不到或數字存疑時，回 MOPS 對照。亦可改用 TWSE / TPEx OpenAPI。

> ⚠️ **合約負債（ContractLiabilities）注意**：這是 IFRS 15 科目，FinMind 的資產負債表是以各公司申報的 XBRL 科目名回傳，欄位 `type` 字串可能因公司而異（如 `ContractLiabilitiesCurrent`、`合約負債－流動`）。**Phase 1 必須先實際拉幾家公司的資產負債表，列出真實的 type 清單再寫對應表**，不要憑空假設欄位名。

### 財報公布時程（決定資料更新節奏）
- 月營收：每月 **10 日**前公布上月數字。
- 第一季財報：**5/15** 前。
- 第二季（半年報）：**8/14** 前。
- 第三季：**11/14** 前。
- 年報（Q4）：隔年 **3/31** 前。

ETL 排程貼著這些日期跑即可，平時不需頻繁更新。

---

## 3. 指標定義與「獲利」的拆解

「獲利」不是單一數字，建議至少抓這幾層，讓監測能分辨「本業 vs 業外」：
- 營業毛利 / 毛利率
- 營業利益 / 營益率（最能反映本業）
- 稅後淨利（本期淨利）
- 母公司業主淨利
- EPS（每股盈餘）

監測時以**營業利益**與**稅後淨利**為主軸，毛利率作為品質輔助欄位。

---

## 4. 產業別分類

- 來源：`TaiwanStockInfo` 的 `industry_category`。
- 預設用官方分類，但因為你關注的是 AI 供應鏈主題（被動元件、CCL、ABF、銅箔、光通訊、記憶體…），這些在官方分類裡會散落在「電子零組件」「電腦及週邊」等大類。
- 建議做一層**自訂主題分類映射表**（`industry_map.json`）：把官方產業別 + 個股代號，對應到你自己的主題群組。例如：
  - `記憶體`：2344 華邦電、南亞科…
  - `CCL/PCB 材料`：2383 台光電、6274 台燿、8358 金居、3037 欣興、8046 南電…
  - `被動元件`：2327 國巨、3042 晶技、2484 希華…
- 儀表板同時支援「官方產業別」與「自訂主題」兩種分組切換。

### 邊界情況
- **金融股**沒有存貨、合約負債意義不同 → 該產業群組隱藏這兩欄。
- **營建股**的「存貨」是土地與待售房屋，性質特殊 → 標註提醒，不與製造業同標準比較。

---

## 5. 「大幅變動」偵測邏輯（核心價值）

### 5.1 基本變動率
對每個指標，每期計算：
- **QoQ（季增率）**：對季資料（應收、存貨、合約負債、季營收、獲利）。
- **YoY（年增率）**：所有指標都算，可避開季節性。
- **MoM（月增率）+ 月營收 YoY**：對月營收（這是台股獨有的領先資訊，務必善用）。

### 5.2 門檻與標記等級（可在設定檔調整）
| 等級 | YoY 條件 | 視覺 |
|------|---------|------|
| 🔴 強烈 | \|YoY\| ≥ 50% | 深色底 + 動畫/邊框 |
| 🟠 顯著 | 30% ≤ \|YoY\| < 50% | 中度底色 |
| 🟡 留意 | 15% ≤ \|YoY\| < 30% | 淺底色 |
| ⚪ 一般 | < 15% | 無標記 |

上升用暖色（紅／橘）、下降用冷色（綠／藍），用**色彩深淺做 heatmap**反映幅度。

### 5.3 進階交叉訊號（讓監測有判讀力，而非只看單一數字）
單看一個指標漲跌容易誤判，加上幾條交叉規則會大幅提升價值：

- **合約負債大增**：通常代表客戶預付／在手訂單增加，是**未來營收的領先指標**。標為正向亮點（對你追的 AI 供應鏈題材特別有用）。
- **存貨 ↑ 但營收持平／下滑**：可能是滯銷或需求轉弱 → 標為**警示**。
- **存貨 ↑ 且營收同步 ↑**：可能是備貨迎接需求 → 標為**中性偏正**。
- **應收帳款增速 > 營收增速**：收款品質惡化或塞貨疑慮 → 標為**警示**。
- **存貨週轉天數、應收週轉天數**惡化（拉長）→ 輔助警示欄位。

這些規則做成獨立的 `signals` 欄位，前端用 icon/tag 呈現，並可單獨篩選。

### 5.4 排序
- 預設：每個產業群組內，依「最新一期 YoY（可選指標）」由大到小排。
- 可切換排序鍵：QoQ、YoY、絕對金額、訊號嚴重度。
- 全域模式：跨產業排出「全市場變動最大 Top N」。

---

## 6. 資料架構與檔案結構

```
project/
├── etl/
│   ├── fetch.py          # 從 FinMind 抓五大指標 + 產業別
│   ├── transform.py      # 清洗、算 QoQ/YoY、跑交叉訊號
│   ├── industry_map.json # 自訂主題分類映射
│   ├── config.json       # 門檻、追蹤清單、API token
│   └── build_json.py     # 輸出前端用的 data.json
├── data/
│   └── data.json         # 前端唯一資料來源（ETL 產出）
├── index.html            # 儀表板（單檔，讀 data/data.json）
└── plan.md
```

### data.json 結構（草案）
```json
{
  "updated_at": "2026-06-22",
  "industries": [
    {
      "name": "記憶體",
      "metrics_enabled": ["ar","inventory","contract_liab","revenue","profit"],
      "stocks": [
        {
          "id": "2344", "name": "華邦電",
          "metrics": {
            "contract_liab": {
              "latest": 1234567, "period": "2026Q1",
              "qoq": 0.18, "yoy": 0.42,
              "history": [/* 近 8 季 */]
            }
            /* ar, inventory, revenue, profit 同結構 */
          },
          "signals": ["contract_liab_surge","inventory_healthy"]
        }
      ]
    }
  ]
}
```
`history` 帶近 8 季／12 個月，供前端畫迷你趨勢圖（sparkline）。

---

## 7. 前端 HTML 設計

### 版面
- 頂部：更新日期、分組切換（官方產業別 / 自訂主題）、指標多選、門檻調整、搜尋框。
- 主體：**依產業群組分區塊**，每區一張可排序表格。
- 全域檢視分頁：跨產業 Top N 變動榜。

### 表格欄位（每檔個股一列）
| 代號 | 名稱 | 指標值（最新期）| QoQ | YoY | 趨勢 sparkline | 訊號 tag |

- 數字格 = heatmap 底色（依 §5.2）。
- 點欄位標題可排序；點個股可展開近 8 季明細。
- 訊號用彩色 tag（如「合約負債暴增」綠標、「應收>營收」紅標）。

### 互動
- 純前端篩選／排序，不重抓資料。
- 「只看有警示」「只看強烈變動」快速濾鏡。
- 響應式，手機可看（你常在手機上看盤）。

### 技術選型
- 單檔 `index.html`，CSS/JS 內嵌。
- 資料：`fetch('data/data.json')`。
- 排序/篩選：原生 JS 即可；表格量大再考慮輕量庫。
- sparkline：用 inline SVG 自己畫，免外部依賴。
- 圖表庫若要用，走 CDN（如 Chart.js）。
- ⚠️ 不使用 localStorage（環境限制），狀態存記憶體即可。

---

## 8. 開發階段

**Phase 1 — 資料探勘（最關鍵）**
- 串 FinMind，實拉 3~5 檔（含 2344）的損益表、資產負債表、月營收。
- **列出資產負債表真實 type 清單**，確認應收帳款／存貨／合約負債的正確欄位名，寫對應表。
- 確認金融、營建等特殊產業的欄位缺漏狀況。

**Phase 2 — ETL**
- 完成 fetch / transform / build_json，算出 QoQ、YoY、交叉訊號，產出 data.json。
- 建 industry_map.json 與追蹤清單。

**Phase 3 — 前端骨架**
- 讀 data.json，依產業分區出可排序表格 + heatmap 底色。

**Phase 4 — 標記與訊號**
- 套用門檻分級、交叉訊號 tag、Top N 變動榜、篩濾鏡。

**Phase 5 — 打磨**
- sparkline 趨勢、展開明細、響應式、設定檔化門檻。

---

## 9. 注意事項與風險

- **資料延遲**：財報季才更新，月營收每月一次；儀表板要清楚標示「資料截至 = 最新財報期別」，避免誤判即時性。
- **合約負債覆蓋率**：非所有公司都有此科目；缺值要顯示「—」而非 0，否則 YoY 會算出假訊號。
- **基期失真**：YoY 在去年基期極小時會爆出超大百分比（如合約負債從 100 萬→500 萬）→ 對絕對金額過小者降權或加註，避免排序被雜訊洗版。
- **FinMind 額度**：免費版有每日請求上限；ETL 要做快取，只增量更新最新一期，不要每次全量重抓。
- **產業比較基準**：不同產業的存貨／應收結構天差地遠，**只在同產業內做相對比較**，跨產業排序僅看變動率不看絕對水準。

---

## 10. 後續可擴充
- 加入存貨週轉天數、應收週轉天數的趨勢監測。
- 合約負債 → 次季營收的領先相關性回測。
- Email／Telegram 推播：跑完 ETL 後自動把當期強烈變動清單推給你。